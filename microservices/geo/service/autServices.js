// === === === === === === === === === === === ===
// Authorization
// === === === === === === === === === === === ===

class Authorization {
  constructor(options) {}
}

export default Authorization
